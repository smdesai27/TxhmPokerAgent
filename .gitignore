venv/
wandb/
__pycache__/
*.pyc
.DS_Store
.env
.idea/
.vscode/

# Generated / build artifacts
*.egg-info/

# Docs: ignore stray markdown by default, but track the curated ones.
*.md
!README.md
!docs/*.md

# Experiment scratch — results live on the HPC / W&B, not in git.
logs/
artifacts/

# Checkpoints: track only the certified champion snapshot + its registry.
checkpoints/*
!checkpoints/checkpoint_aliases.json
!checkpoints/checkpoint_index.json
!checkpoints/snapshots/
checkpoints/snapshots/*
!checkpoints/snapshots/champion/
.vercel

# Agent / editor scratch
.claude/
CLAUDE.md
HANDOFF.md
