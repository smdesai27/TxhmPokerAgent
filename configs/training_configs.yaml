default:
    GAME_NAME: "universal_poker"
    CFR_ITERATIONS: 100000
    LR: 0.0005
    BATCH_SIZE: 512
    HIDDEN_DIM: 512
    USE_LAYERNORM: true
    USE_RESIDUAL: true
    USE_TARGET_NET: true
    TARGET_UPDATE_TAU: 0.005
    GRAD_CLIP: 1.0
    LR_SCHEDULER: "plateau"
    MAX_CHECKPOINTS: 5

debug:
    CFR_ITERATIONS: 100
    SELF_PLAY_GAMES: 1
    BATCH_SIZE: 32
    BUFFER_SIZE: 1000
    LOG_INTERVAL: 1
    CHECKPOINT_FREQ: 50
    EVAL_FREQ: 50
    HIDDEN_DIM: 64

ablation_low_lr:
    LR: 0.00005

ablation_no_target:
    USE_TARGET_NET: false

ablation_small_net:
    HIDDEN_DIM: 128
    NUM_LAYERS_CARD: 2
    NUM_LAYERS_ACTION: 1

stable_v2:
    LR: 0.0001
    BATCH_SIZE: 1024
    TARGET_UPDATE_TAU: 0.001
    SELF_PLAY_GAMES: 10
    BUFFER_SIZE: 500000
    GRAD_CLIP: 5.0  # Increased since we scaled down values
    USE_TARGET_NET: true
